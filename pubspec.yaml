name: flutter_graph_ql
description: Graph QL playground
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  graphql_flutter: ^5.1.2
  flutter_bloc: ^8.1.3
  flutter_modular: ^5.0.3
  freezed: ^2.4.0
  flutter_hooks: ^0.18.6

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  build_runner: ^2.4.6
  json_serializable: ^6.7.1

flutter:
  uses-material-design: true